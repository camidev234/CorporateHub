import PropTypes from 'prop-types';

export const ShowComments = ({ isLoading, company_id }) => {
    return (
        <div>
            
        </div>
    )
} 

ShowComments.propTypes = {
    isLoading: PropTypes.bool.isRequired,
    company_id: PropTypes.number.isRequired
}